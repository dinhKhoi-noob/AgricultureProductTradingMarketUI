/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}
const a = [1,2,3]
const [n] = a;
console.log(n);

const foo = {
  bar(){
    
  }
}

module.exports = nextConfig
